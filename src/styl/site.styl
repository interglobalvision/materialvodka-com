@import '~kouto-swiss/index.styl'
@import '~globie-styl-library/helpers'

// ******************************************************

// RESETS

meyer-reset()
box-sizing-reset()
normalize()


// ******************************************************

// GRID

@import '~globie-styl-library/grid'

// ******************************************************

// FONTS
@import 'fonts/fonts'

//$font-sans = 'TradeGothic LT Light', font-stack('Helvetica')
//$font-bold = 'TradeGothic LT Bold', font-stack('Helvetica')
//$font-regular = 'TradeGothic LT', font-stack('Helvetica')
$font-sans = 'Folio BQ Light', font-stack('Helvetica')
$font-sans-italic = 'Folio BQ Light Italic', font-stack('Helvetica')
$font-bold = 'Folio BQ Bold', font-stack('Helvetica')
$font-medium = 'Folio BQ Medium', font-stack('Helvetica')
$font-serif = font-stack('Georgia')

// ******************************************************


// TYPOGRAPHY

@import '~globie-styl-library/type'

$font-size-larger = 1.3rem

$font-leading-basic = 1.35
$font-leading-tighter = 1.15
$font-leading-larger = 1.45

$font-tracking-basic = .001em
$font-tracking-wider = .05em

html
  font-family: $font-sans
  letter-spacing: $font-tracking-basic
  font-weight: 300

body
  line-height: $font-leading-basic

.font-sans
  font-family: $font-sans

.font-medium
  font-family: $font-medium
  font-weight: 400

.font-serif
  font-family: $font-serif

.font-larger
  font-size: $font-size-larger

.line-tighter
  line-height: $font-leading-tighter

.line-larger
  line-height: $font-leading-larger

em, .font-italic
  font-style: italic
  font-family: $font-sans-italic

strong
  font-family: $font-medium
  font-weight: 400

.font-bold
  font-family: $font-bold
  font-weight: 500

// ******************************************************

// VARIABLES

$color-black = rgb(0, 0, 0)
$color-white = rgb(255, 255, 255)

// ******************************************************

// BASIC

html
  color: $color-white
  background-color: $color-black

img
  height: auto
  max-width: 100%

.mobile-only
  display: initial !important

.desktop-only
  display: none !important

.font-outline
  text-stroke: 1px $color-white
  -webkit-text-stroke: 1px $color-white
  color: transparent

// ******************************************************

// MEDIA QUERIES

@import 'responsive/mobile'

@import 'responsive/tablet-portrait'

@import 'responsive/tablet-landscape'

@import 'responsive/laptop'

@import 'responsive/desktop'

@import 'responsive/hd'
