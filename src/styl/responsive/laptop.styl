@media screen and (min-width: 1280px)

  $gutter = 40px

  grid-maker(100%, $gutter, l)

  // ******************************************************

  // PRIZE

  .prize-portrait
    height: calc('25vw - ' + $gutter)
